# Agent brief — T016

**Lane:** docs | **Phase:** docs

## Task
E2E manual test checklist

Add tasks/e2e-checklist.md with step-by-step: join request, DM link, verify page, approve, ban, rejoin block. No code changes required beyond doc.

## Acceptance criteria
- tasks/e2e-checklist.md exists with >= 10 numbered steps
- Covers ban evasion scenario

## Verification (must pass before complete)
```powershell
python -m orchestrator verify T016
```

Task-only command:
```
test -f tasks/e2e-checklist.md
```

## Workflow
1. Implement ONLY this task. No other PRD items.
2. Run `python -m orchestrator verify T016`
3. If pass: `python -m orchestrator complete T016`
4. If fail: `python -m orchestrator fail T016 --reason "<stderr summary>"`
5. Never commit `.env` or secrets.

## Project goals
- Goal 1 — The Gate: catch banned users rejoining on new accounts at verification
- Goal 2 — The Watcher: stylometry profiles flag members matching banned users

## Recent lessons
# Orchestrator lessons

Failures and patterns the agent must not repeat. Updated automatically on `orchestrator fail` and manually after corrections.

## Format

```
### YYYY-MM-DD — TASK_ID — short title
- **Symptom:** what broke
- **Root cause:** why
- **Fix:** what worked
- **Guard:** test or check that prevents recurrence
```

---


## Stack
python-telegram-bot, FastAPI, SQLAlchemy, pytest, ruff, Hardhat/Adiri testnet.
Repo root: C:\Users\daroo\repos\Telegram-bot
