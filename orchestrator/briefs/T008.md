# Agent brief — T008

**Lane:** gate | **Phase:** gate

## Task
Structured log channel messages

Update log_to_channel in telegram_actions.py: formatted messages for ELEVATED_RISK, BAN_EVASION, WATCHER_MATCH with risk factors and truncated fingerprint hash. Add unit test for message format strings.

## Acceptance criteria
- Log messages include user id, risk score/factors where applicable
- tests/test_log_format.py passes

## Verification (must pass before complete)
```powershell
python -m orchestrator verify T008
```

Task-only command:
```
.venv\Scripts\pytest tests/test_log_format.py -q
```

## Workflow
1. Implement ONLY this task. No other PRD items.
2. Run `python -m orchestrator verify T008`
3. If pass: `python -m orchestrator complete T008`
4. If fail: `python -m orchestrator fail T008 --reason "<stderr summary>"`
5. Never commit `.env` or secrets.

## Project goals
- Goal 1 — The Gate: catch banned users rejoining on new accounts at verification
- Goal 2 — The Watcher: stylometry profiles flag members matching banned users

## Recent lessons
# Orchestrator lessons

Failures and patterns the agent must not repeat. Updated automatically on `orchestrator fail` and manually after corrections.

## Format

```
### YYYY-MM-DD — TASK_ID — short title
- **Symptom:** what broke
- **Root cause:** why
- **Fix:** what worked
- **Guard:** test or check that prevents recurrence
```

---


## Stack
python-telegram-bot, FastAPI, SQLAlchemy, pytest, ruff, Hardhat/Adiri testnet.
Repo root: C:\Users\daroo\repos\Telegram-bot
