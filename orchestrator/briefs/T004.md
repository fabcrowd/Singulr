# Agent brief — T004

**Lane:** test-harness | **Phase:** test-harness

## Task
Shared pytest fixtures for async DB

Add tests/conftest.py with session-scoped async SQLite engine, tables created per test, dependency override for get_session in FastAPI tests.

## Acceptance criteria
- tests/conftest.py exists
- T001-T003 use shared fixtures without duplication

## Verification (must pass before complete)
```powershell
python -m orchestrator verify T004
```

Task-only command:
```
.venv\Scripts\pytest -q
```

## Workflow
1. Implement ONLY this task. No other PRD items.
2. Run `python -m orchestrator verify T004`
3. If pass: `python -m orchestrator complete T004`
4. If fail: `python -m orchestrator fail T004 --reason "<stderr summary>"`
5. Never commit `.env` or secrets.

## Project goals
- Goal 1 — The Gate: catch banned users rejoining on new accounts at verification
- Goal 2 — The Watcher: stylometry profiles flag members matching banned users

## Recent lessons
# Orchestrator lessons

Failures and patterns the agent must not repeat. Updated automatically on `orchestrator fail` and manually after corrections.

## Format

```
### YYYY-MM-DD — TASK_ID — short title
- **Symptom:** what broke
- **Root cause:** why
- **Fix:** what worked
- **Guard:** test or check that prevents recurrence
```

---


## Stack
python-telegram-bot, FastAPI, SQLAlchemy, pytest, ruff, Hardhat/Adiri testnet.
Repo root: C:\Users\daroo\repos\Telegram-bot
