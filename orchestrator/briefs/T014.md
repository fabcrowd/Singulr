# Agent brief — T014

**Lane:** frontend | **Phase:** frontend

## Task
Verification page environment consistency check

In static/verify.js detect obvious VM/automation signals (webdriver, headless UA). Send env_flags to submit payload; backend adds +20 risk factor env_anomaly (flag not block). Test backend scoring.

## Acceptance criteria
- submit accepts env_flags field
- webdriver true adds env_anomaly to risk_factors on flag path
- Test in test_api_verify.py

## Verification (must pass before complete)
```powershell
python -m orchestrator verify T014
```

Task-only command:
```
.venv\Scripts\pytest tests/test_api_verify.py -q
```

## Workflow
1. Implement ONLY this task. No other PRD items.
2. Run `python -m orchestrator verify T014`
3. If pass: `python -m orchestrator complete T014`
4. If fail: `python -m orchestrator fail T014 --reason "<stderr summary>"`
5. Never commit `.env` or secrets.

## Project goals
- Goal 1 — The Gate: catch banned users rejoining on new accounts at verification
- Goal 2 — The Watcher: stylometry profiles flag members matching banned users

## Recent lessons
# Orchestrator lessons

Failures and patterns the agent must not repeat. Updated automatically on `orchestrator fail` and manually after corrections.

## Format

```
### YYYY-MM-DD — TASK_ID — short title
- **Symptom:** what broke
- **Root cause:** why
- **Fix:** what worked
- **Guard:** test or check that prevents recurrence
```

---


## Stack
python-telegram-bot, FastAPI, SQLAlchemy, pytest, ruff, Hardhat/Adiri testnet.
Repo root: C:\Users\daroo\repos\Telegram-bot
